<div class="container">
  <div class="booking-confirmation">
    <div class="show-cards">
      <div class="card-description">
        <div class="booking-header border-bottom mb-4">
          <h2><i class="fas fa-check-circle"></i> Booking request sent successfully</h2>
          <p id="booking-status">Status: <%= @booking.status.capitalize %></p>
        </div>
        <div class="booking-info">
          <p>Thank you for booking my <span style="font-weight: bold;"><%= @lecture.title %></span> class.</p>
          <p>I'll contact you as soon as possible to confirm the date and time. In the mean time, if you have any questions, please don't hesitate to contact me 😉</p>
          <p>I am looking forward to seeing you!</p>
        </div>
      </div>

      <div class="card-info">
        <h3>Booking details</h3>
        <div class="info-details">
          <p><i class="far fa-calendar-alt"></i> <span> <%= @booking.desired_time.strftime("%A, %d %b %Y") %></span> </p>
          <p><i class="far fa-clock"></i> <span> <%= @booking.desired_time.strftime("%I:%M %p") %></span> </p>
          <p><i class="fas fa-money-bill-wave"></i> <span> <%= @lecture.price %> € </span></p>
          <% if @booking.pending? %>
            <%= link_to 'Cancel request', booking_path(@booking), method: :delete, data: { confirm: 'Do you really want to cancel your request?' }, class: "btn btn-block btn-flat btn-lg mb-3", id: "delete-button" %>
          <% end %>
        </div>
        <div class="button-container">
          <%= link_to 'Contact me', new_contact_path, class: "btn btn-block btn-flat btn-lg mb-3" %>
          <%= link_to 'Back to list', lectures_path, class: "btn btn-block btn-flat btn-lg" %>
        </div>
      </div>
    </div>
  </div>
</div>


